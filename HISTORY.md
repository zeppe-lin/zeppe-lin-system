# History

## 0.1.0 — unreleased

- Establish the pinned native controller source set.
- Build `pkgctl` and `pkgstate-init` directly from Meson subproject targets.
- Qualify external host dependencies without a private installed-prefix loop.
- Record canonical historical rootfs seed descriptors for later bootstrap work.
